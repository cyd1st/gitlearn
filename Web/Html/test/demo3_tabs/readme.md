使用 :checked 伪类配合隐藏的 radio input 是更强大、更常用的纯 CSS 标签页实现方式，无需依赖 URL 锚点，体验更好。

优点（相比 `:target`）：

| 特性 | `:checked` 方案 | `:target` 方案 |
|------|------------------|----------------|
| 不改变 URL | ✅ 是 | ❌ 会添加 #tab |
| 可默认选中 | ✅ 容易 | ❌ 需 JS 或初始 hash |
| 无页面跳转 | ✅ | ❌ 点击会滚动到锚点 |
| 更适合复杂布局 | ✅ | ❌ 受限于 DOM 结构 |